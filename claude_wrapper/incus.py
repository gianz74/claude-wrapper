"""incus CLI helpers (DESIGN §2). incus-only — no LXD/lxc.

``cli_run``/``cli_quiet``, ``container_exists``, device add/remove/show,
``copy``/``launch``/``start``, idmap/apparmor/config set.

Implemented in T3.
"""
