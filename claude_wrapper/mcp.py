"""MCP / IDE bridge (DESIGN §12), parameterized by the selected instance.

``--mcp-config`` file staging + mount, loopback proxy devices, uid-1000
sentinel, IDE lockfile pid + trailing-slash patches.

Implemented in T9.
"""
